{
  "name": "@qmaru/mdp-js",
  "version": "1.1.3",
  "description": "js mdp image urls",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "bin": {
    "mdp-js": "dist/cli.js"
  },
  "scripts": {
    "build:node:lib": "bun build src/index.ts --outdir dist --target node --sourcemap=external",
    "build:node:cli": "bun build src/cli.ts --outdir dist --target node --sourcemap=external",
    "build:node": "bun run build:node:lib && bun run build:node:cli",
    "build:web": "bun build src/index.ts --outdir dist/web --target browser --minify --sourcemap=external",
    "build:types": "bun run tsc --emitDeclarationOnly --outDir dist",
    "build": "bun run build:types && bun run build:node && bun run build:web",
    "test": "bun test",
    "dev": "bun run src/cli.ts",
    "lint": "oxlint",
    "fmt": "oxfmt"
  },
  "files": [
    "dist"
  ],
  "author": "qmaru",
  "license": "ISC",
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "oxfmt": "^0.16.0",
    "oxlint": "^1.31.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "node-html-parser": "^7.0.1"
  },
  "publishConfig": {
    "@qmaru:registry": "https://npm.pkg.github.com",
    "access": "public"
  }
}